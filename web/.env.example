VITE_API_BASE_URL=
# Leave empty for local `npm run dev` (Vite proxies /api → localhost:8080).
# Required for production / Render Static Site builds, e.g.:
# VITE_API_BASE_URL=https://food-investigators-api.onrender.com

