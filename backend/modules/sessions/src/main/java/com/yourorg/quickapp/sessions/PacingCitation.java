package com.yourorg.quickapp.sessions;

/**
 * One calm, parent-facing pacing citation (plain-language source label — not a
 * clinical bibliography entry).
 */
public record PacingCitation(String title, String source) {}
