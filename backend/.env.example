# Local Gemini config for `./gradlew :backend:bootRun`.
# Copy to backend/.env (gitignored) and fill in the key.
GEMINI_API_KEY=
# Optional overrides:
# GEMINI_MODEL=gemini-3.5-flash
# GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com/v1beta
